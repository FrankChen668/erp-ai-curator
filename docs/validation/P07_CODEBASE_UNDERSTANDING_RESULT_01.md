# P07 — Codebase / Program → Understand Logic, Support FS, Locate Defects

Date: 2026-08-30

## Status

> **INVALIDATED — DO NOT USE AS PRODUCT EVIDENCE**

This file previously contained a `CLOSED` verdict produced during a compressed long-context sprint.

That verdict is withdrawn.

## Why invalidated

The prior sprint result relied on broad claims about:

- Claude Code large-repository practice;
- OpenAI/Codex engineering practice;
- current practitioner workflows;
- legacy-system documentation practice;
- code-Agent reliability boundaries.

However, the retained authority did not preserve concrete source URLs/citations or an auditable acquisition trail for those claims.

Therefore it is not possible to establish which claims were actually researched and which were model synthesis. That is insufficient for a product-evidence `CLOSED` decision.

## What is not currently established

Do **not** treat any of the following as accepted project conclusions:

- repo-aware code Agent is always the correct generic default;
- a specialist code-understanding/graph/architecture capability is generally unnecessary;
- current major code-Agent ecosystems already resolve the relevant adoption boundary;
- no runtime/local delta could change the decision.

These may later prove true, partially true or false after a trustworthy rerun.

## Correct future action

P07 should only be rerun after the current P03 card is completed reliably, unless a real supplied artifact changes priority.

A trustworthy P07 run must retain:

- concrete practitioner/current-source URLs;
- evidence-role labels;
- actual implementation/tool capability evidence;
- limitations/counter-evidence;
- a clear distinction between code facts, inferred business meaning and validated intended behavior;
- a justified stop decision.

The previous content remains available in Git history for audit, but it is not current authority.
